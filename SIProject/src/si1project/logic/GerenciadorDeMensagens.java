package si1project.logic;

/**
 * A responsabiliade principal dessa
 * classe eh identificar o remetente
 * e o destinatario de cada metodo,
 * transformar a chamada de metodo em
 * uma Mensagem e ligar remetente e 
 * destinatario atraves dessa mensagem.
 * Cada mensagem necessariamente vai estar associada
 * a uma Carona na lista de caronas de ambos
 * os usuarios (remetente e destinatario).
 */
public class GerenciadorDeMensagens {

	public Object getAtributoSolicitacao(int idSolicitacao, String atributo) throws Exception {
		//TODO
		//return GerenciadorDeDados.mapaIdMensagem.get(idSolicitacao).getAtributo(atributo);
		return null;
	}

}
